[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "*.py", "Dockerfile", "requirements.txt", "railway.toml"]

[deploy]
startCommand = "python start_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
