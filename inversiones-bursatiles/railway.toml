[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Railway detecta el puerto automáticamente desde EXPOSE
# No se necesita healthcheck HTTP ya que es un worker process
